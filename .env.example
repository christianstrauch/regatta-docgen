# Database Configuration
DATABASE_PATH=/home/user/data/regatta.db

# OIDC Authentication Configuration
# The application will automatically discover endpoints from OIDC_ISSUER/.well-known/openid-configuration
OIDC_ISSUER=https://your-oidc-provider.com
OIDC_CLIENT_ID=your-client-id
OIDC_CLIENT_SECRET=your-client-secret
OIDC_CALLBACK_URL=http://localhost:3000/api/auth/callback

# OIDC Scope Configuration
# Space-separated list of scopes to request from the OIDC provider (default: 'openid profile email')
# IMPORTANT: Add any custom scopes required to receive the race committee claim
# Example: OIDC_SCOPE=openid profile email groups (if race committee is in 'groups' claim)
OIDC_SCOPE=openid profile email

# OIDC Claims Configuration
# Specify which claim to use for user identification (default: 'sub')
OIDC_USER_ID_CLAIM=sub
# Specify which claim contains the race committee identifier (default: 'org')
# This claim must be included in the ID token via the scopes configured above
OIDC_RACE_COMMITTEE_CLAIM=org
# Optional: Specify which claim contains the race committee logo URL
# If provided and the claim exists, it will override the default LOGO_URL for that race committee
# Example: OIDC_RACE_COMMITTEE_LOGO_CLAIM=logo_url
OIDC_RACE_COMMITTEE_LOGO_CLAIM=

# Application Configuration
# Default logo URL used when no race committee-specific logo is provided
LOGO_URL=https://example.com/logo.png
APP_NAME=regatta.club Document Generator
NODE_ENV=production
